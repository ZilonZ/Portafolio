# ZilonZ — Portfolio Personal

Portafolio de Cristian Morales (ZilonZ), desarrollador web fullstack con enfoque en React y JavaScript.

## Estructura de archivos

```
/
├── index.html          # Página principal
├── style.css           # Estilos (cyberpunk/gaming)
├── script.js           # Interactividad y efectos
├── Foto_CV_Relax.jpeg  # Foto real (aparece por defecto)
├── Avatar_ZilonZ.png   # Avatar gaming (aparece al hover)
└── 1778534735306_image.png  # Screenshot del proyecto GEMA
```

## Cómo subir a GitHub Pages

1. Crea un repositorio en GitHub llamado `portfolio` (o `<tu-usuario>.github.io` para que sea tu página principal).

2. Sube todos los archivos a la raíz del repositorio:
   ```bash
   git init
   git add .
   git commit -m "feat: portfolio inicial ZilonZ"
   git branch -M main
   git remote add origin https://github.com/TU_USUARIO/TU_REPO.git
   git push -u origin main
   ```

3. En el repositorio ve a **Settings → Pages**, selecciona la rama `main` y la carpeta `/root`. Guarda.

4. En 1–2 minutos tu sitio estará en `https://TU_USUARIO.github.io/TU_REPO/`.

## Personalización pendiente

- Actualiza los links de **GitHub** y **LinkedIn** en `index.html` (sección `#contact`).
- Agrega la URL de tu portafolio en el CV cuando esté publicado.
- Si quieres un dominio personalizado (ej. `zilonz.dev`), agrégalo en **Settings → Pages → Custom domain**.

## Tech stack del portafolio

HTML · CSS · Vanilla JavaScript — sin dependencias externas, carga ultrarrápida.
